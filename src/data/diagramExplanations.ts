/**
 * Texto explicativo exibido abaixo de cada diagrama.
 * Descreve o caso de uso representado e o que cada elemento do diagrama significa.
 */
export const diagramExplanations: Record<string, string> = {
  'load-balancer': `**Caso de uso:** e-commerce com três instâncias do serviço de catálogo. Os **clientes** (browsers ou app) enviam todas as requisições para um único endpoint; o **Load Balancer** recebe o tráfego e distribui cada request para um dos **Server 1, 2 ou 3** (por exemplo, Round Robin ou Least Connections). Nenhum cliente "conhece" o IP de um servidor específico — só o do LB. Se um servidor cair, o health check do LB o remove do pool e o tráfego segue apenas para os saudáveis. Assim você escala horizontalmente: mais tráfego → mais servidores atrás do LB, sem mudar a URL para o usuário.`,

  cache: `**Caso de uso:** API de perfil de usuário. A aplicação recebe muitas leituras do mesmo perfil (ex.: página do usuário, header com nome). Em vez de ir ao **banco de dados** em toda request, ela consulta primeiro o **Redis** (camada de cache). Se o dado estiver no cache (**hit**), a resposta sai em ~1 ms; se não (**miss**), a app busca no banco, grava no cache com TTL (ex.: 5 min) e devolve. O diagrama mostra o fluxo: request → cache → em miss, origem (DB). Com isso a carga no banco cai e a latência p99 diminui. A invalidação (ex.: usuário atualizou o nome) é feita ao escrever: atualiza o banco e remove ou atualiza a chave no cache.`,

  database: `**Caso de uso:** sistema com alta leitura e dados que precisam crescer além de um único nó. O **primário** recebe todas as escritas e replica para as **réplicas** via log (WAL). As leituras (listagens, relatórios, buscas) são roteadas para as réplicas, aliviando o primário. O **sharding** (particionamento) divide os dados por chave (ex.: \`user_id\`) em vários bancos: cada shard guarda um subconjunto. A aplicação (ou proxy) calcula qual shard usar para cada query e roteia. Assim você escala leitura (mais réplicas) e escrita/armazenamento (mais shards). O diagrama mostra o primário, as réplicas e os shards com dados distribuídos.`,

  microservices: `**Caso de uso:** plataforma com **API Gateway** como ponto de entrada único; o gateway roteia para os microsserviços **Auth** (login, tokens), **Users** (perfil, preferências), **Orders** (pedidos) e **Notifications** (e-mail, push). Cada serviço tem sua equipe, deploy e banco independentes. O cliente (web/mobile) chama apenas o gateway; ele decide, por path ou regra, qual backend invocar. Em produção cada caixa seria um conjunto de réplicas atrás de um LB. O diagrama ilustra a topologia típica: uma camada de gateway e, abaixo, os domínios desacoplados.`,

  cdn: `**Caso de uso:** entrega de **assets estáticos** (JS, CSS, imagens) e **streaming de vídeo**. O **cliente** solicita uma URL (ex.: \`cdn.empresa.com/video.mp4\`). O DNS resolve para um **PoP** (ponto de presença) da CDN próximo ao usuário. Se o recurso estiver em cache no edge (**hit**), a resposta vem do PoP; se não (**miss**), o edge busca da **origin** (seu servidor ou bucket S3), armazena e atende as próximas requisições. O diagrama mostra usuários geograficamente distribuídos sendo atendidos pelo edge mais próximo, reduzindo latência e carga na origin.`,

  'message-queue': `**Caso de uso:** processamento assíncrono de **pedidos**. O **produtor** (API de pedidos) publica uma mensagem "Pedido criado" na **fila** (Kafka, SQS) e responde 200 ao usuário imediatamente. Um ou mais **consumidores** (workers) leem da fila e processam: enviar e-mail, atualizar estoque, notificar parceiro. Cada mensagem é consumida por **um** consumidor (competing consumers). Se o consumidor falhar, a mensagem volta a ficar visível (ou vai para DLQ após N tentativas). O diagrama mostra o fluxo unidirecional: produtor → fila → consumidor; a fila desacopla e absorve picos.`,

  'api-gateway': `**Caso de uso:** app mobile e web chamam **um** domínio (\`api.empresa.com\`). O **API Gateway** recebe a requisição, valida o token JWT (**Auth**), aplica **rate limit** e roteia por path: \`/api/users\` → serviço **Users**, \`/api/orders\` → **Orders**. Os backends não são expostos à internet; só o gateway. O diagrama mostra **Clientes** (browsers, apps) no topo; no meio o gateway com as funções roteamento • auth • rate limit; abaixo os três microsserviços. Uma request típica: GET /api/orders com Bearer token → gateway valida → encaminha para Orders e devolve a resposta.`,

  'rate-limiting': `**Caso de uso:** proteção da API contra abuso e garantia de uso justo. **Muitas requisições** (de um mesmo IP ou user_id) chegam ao **Rate Limiter** (ex.: implementado no gateway com Redis). O limiter conta as requisições na janela (ex.: 100/min); enquanto estiver dentro do limite, a request segue para o **backend** (resposta 200); ao exceder, retorna **429 Too Many Requests** sem chamar o backend. O diagrama mostra o fluxo: requisições → rate limiter → OK ou 429. Algoritmos comuns: token bucket (permite burst), sliding window (mais justo). Por IP, por user_id após login ou por API key para planos (free vs premium).`,

  'service-discovery': `**Caso de uso:** ambiente com **Kubernetes** ou containers onde os IPs dos serviços mudam a cada deploy. O **cliente** (outro serviço ou o gateway) não sabe o IP do "serviço de usuários". Ele consulta o **Registry** (Consul, etcd ou o próprio K8s Service) pelo nome lógico (ex.: \`users\`). O registry devolve a lista de **instâncias** saudáveis (IP:porta). O cliente escolhe uma (round-robin, random) e chama. No diagrama: Cliente → Registry → Instâncias A e B. O registry é atualizado quando pods sobem (registro) e descem (desregistro) e por health checks.`,

  'circuit-breaker': `**Caso de uso:** o **Cliente** (ex.: serviço de checkout) chama um serviço downstream (ex.: pagamentos). Se o serviço de pagamentos estiver lento ou fora, cada request esperaria o timeout (ex.: 30 s) e acumularia threads. O **Circuit Breaker** fica entre o cliente e o **Serviço**: em estado **Closed**, as chamadas passam; após N falhas consecutivas, o circuito **abre** e as chamadas falham imediatamente (fallback) sem ir à rede; após um tempo (**Half-open**), uma chamada de teste é permitida. O diagrama mostra Cliente → Circuit Breaker → Serviço e a lógica Closed → Open → Half-open, evitando cascata de falhas.`,

  'consistent-hashing': `**Caso de uso:** cache distribuído (ex.: Redis Cluster) ou sharding. As **chaves** (ex.: \`user:123\`) precisam ser atribuídas a um dos **nós** (A, B, C). No **anel** de consistent hashing, cada nó ocupa uma posição; cada chave é mapeada ao primeiro nó no sentido horário a partir do hash da chave. Ao **adicionar** um nó D, apenas as chaves entre o nó anterior e D mudam de dono — uma fração 1/N, em vez de remapear tudo. O diagrama mostra o anel com os três nós; virtual nodes (vnodes) tornam a distribuição mais uniforme.`,

  'object-storage': `**Caso de uso:** upload e entrega de **arquivos** (fotos de perfil, documentos, vídeos). A **App** envia PUT com o arquivo para o **Object Storage** (S3, GCS); o objeto é identificado por **bucket + key** (ex.: \`my-bucket/photos/user123/avatar.jpg\`). O storage armazena o **blob** de forma durável e escalável. Leituras são GET pela mesma key. Objetos são imutáveis: "atualizar" = novo PUT (e opcional versionamento). O diagrama mostra App → Object Storage (S3/GCS) → Blob; ideal para grandes volumes, backups e assets estáticos, não para transações ou queries.`,

  search: `**Caso de uso:** busca por texto em **produtos** ou **documentos**. O usuário digita "tênis corrida". A **Query** é enviada ao **Elasticsearch** (ou OpenSearch), que usa **índice invertido**: para cada termo, a lista de documentos que o contêm; os resultados são ranqueados por relevância (ex.: BM25). O diagrama mostra Query → Elasticsearch → Documentos rankeados. O índice é alimentado por um pipeline (batch ou CDC) a partir do banco transacional; a busca é eventualmente consistente com as escritas.`,

  websockets: `**Caso de uso:** **chat** ou **notificações em tempo real**. O **Cliente** (navegador ou app) abre uma conexão **WebSocket** com o **Servidor** (uma única conexão TCP persistente). Diferente de HTTP request-response, ambos podem enviar **frames** a qualquer momento: o servidor "empurra" novas mensagens sem o cliente fazer polling. O diagrama mostra Cliente ↔ WebSocket (camada de protocolo) ↔ Servidor, bidirecional. Para escalar horizontalmente, usa-se um backplane (ex.: Redis Pub/Sub) para que uma instância possa enviar mensagem a um usuário conectado em outra instância.`,

  pubsub: `**Caso de uso:** evento **"Pedido criado"** precisa acionar vários sistemas. O **Publicador** (serviço de pedidos) envia a mensagem uma vez para o **Tópico** (Kafka, SNS). **Vários assinantes** recebem a mesma mensagem: Assinante 1 (e-mail), Assinante 2 (analytics), Assinante 3 (estoque). O publicador não sabe quem consome; desacoplamento total. O diagrama mostra Publicador → Tópico → N assinantes. Diferente de fila (uma mensagem = um consumidor), no Pub/Sub cada assinante recebe sua cópia; ideal para broadcast e eventos de domínio.`,

  'cap-theorem': `**Caso de uso:** em uma **partição de rede** (ex.: datacenter A perde conexão com o B), você não pode garantir ao mesmo tempo **Consistência** (toda leitura vê a escrita mais recente) e **Disponibilidade** (toda request recebe resposta). O diagrama mostra **C**, **A** e **P** (Partição inevitável). Escolha **CP**: em partição, rejeite escritas/leituras para manter consistência (ex.: banco com réplica síncrona). Escolha **AP**: cada lado continua respondendo com o que tem, aceitando consistência eventual (ex.: Dynamo, Cassandra). O texto abaixo reforça: em partição, escolha CP ou AP.`,

  idempotency: `**Caso de uso:** o cliente envia "Criar pedido" e a rede falha antes da resposta; o cliente **retenta** com a mesma intenção. Sem idempotência, dois pedidos seriam criados. Com **Idempotency Key** (ex.: UUID no header), o servidor na primeira vez processa e guarda a resposta associada à chave; na retentativa com a mesma chave, devolve a resposta guardada sem reprocessar. O diagrama (fluxo genérico Cliente → Idempotency Key → Serviço) representa que a chave fica "no meio" da requisição para deduplicar. Essencial em pagamentos e filas at-least-once.`,

  observability: `**Caso de uso:** diagnóstico em sistema distribuído. Uma request passa por **Métricas** (RED: requests, errors, duration), **Logs** (eventos com trace_id para correlacionar) e **Traces** (propagação da request entre serviços com tempo por hop). O diagrama mostra os três pilares como nós conectados. Com isso você responde: "onde está a latência?" (trace), "qual a taxa de erro do serviço X?" (métricas), "o que aconteceu no request R?" (logs filtrados por trace_id). Alertas e runbooks fecham o ciclo.`,

  bff: `**Caso de uso:** a tela **"Meu perfil"** no app precisa de: usuário, últimos pedidos e preferências. Sem BFF, o cliente faria 3 chamadas. Com **BFF**, o cliente faz **uma** request ao BFF; o BFF chama em paralelo **Users**, **Orders** e **Notifications** (ou o serviço de preferências), agrega o JSON e devolve. O diagrama mostra Cliente → BFF → os três backends. O BFF é otimizado por tipo de cliente (ex.: BFF Web vs BFF Mobile com payloads diferentes). Abaixo: "BFF agrega chamadas e devolve payload otimizado".`,

  'saga-pattern': `**Caso de uso:** **Criar pedido** exige: 1) Reservar estoque, 2) Cobrar pagamento, 3) Criar pedido. Cada passo é um serviço/banco diferente — não há transação global. A **Saga** executa os passos em ordem; se o passo 2 (Cobrar) falhar, a **compensação** do passo 1 (Liberar reserva) é executada. O diagrama mostra o fluxo linear Reservar → Cobrar → Criar pedido → OK e, em vermelho, o caminho de compensação quando falha no passo 2. Sagas podem ser orquestradas (um coordenador) ou coreografadas (eventos).`,

  'cqrs-event-sourcing': `**Caso de uso:** **Comandos** (escrever) vão para o **Event Store** (log append-only de eventos). Os **Read Models** (views para listagens, busca, dashboard) são atualizados por **projectors** que consomem os eventos. O diagrama mostra Comandos → Event Store → (eventos) → View 1 e View 2; à direita, Queries → Read Models. Assim escrita e leitura têm modelos otimizados separados; o estado atual é derivado da sequência de eventos. Ideal quando há múltiplas formas de ler os mesmos dados ou quando auditoria e replay são críticos.`,

  'strangler-fig': `**Caso de uso:** migração **gradual** do monolito para microsserviços. Os **Clientes** continuam chamando o mesmo endpoint. O **API Gateway** na frente roteia: rotas antigas (ex.: \`/users\`, \`/orders\`) ainda vão para o **Monolito**; rotas novas (ex.: \`/notifications\`) vão para o **Novo serviço**. Com o tempo, mais rotas são migradas até o monolito ser desligado. O diagrama mostra Gateway com duas pernas: uma para o monolito, outra para o novo serviço. Sem big bang e com rollback por rota.`,

  'database-per-service': `**Caso de uso:** **Serviço A** (ex.: Users) tem seu **DB A**; **Serviço B** (Orders) tem seu **DB B**. Nenhum serviço acessa o banco do outro; a comunicação é só via **API** (chamadas HTTP/gRPC). O diagrama mostra os dois serviços cada um com seu banco e a seta de API entre eles. Para operações que envolvem os dois (ex.: "criar pedido" precisa validar usuário), o Serviço B chama a API do Serviço A; transações distribuídas são substituídas por sagas e consistência eventual.`,

  'retry-timeout': `**Caso de uso:** o **Cliente** (ex.: API Gateway) chama um serviço downstream. A chamada pode falhar por timeout ou 5xx transitório. O **Retry** com backoff exponencial (+ jitter) tenta de novo algumas vezes antes de desistir. O **Timeout** garante que a thread não fique esperando para sempre; deve ser menor que o timeout do cliente final. O diagrama mostra Cliente → Retry + Timeout → Serviço. Combinar com circuit breaker: após N falhas, parar de retentar por um tempo. O servidor deve ser idempotente para retentativas seguras.`,

  bulkhead: `**Caso de uso:** o mesmo processo chama o **Serviço de Relatórios** (lento) e o **Serviço de Estoque** (crítico para checkout). Sem isolamento, 100 threads bloqueadas em Relatórios podem esgotar o pool e fazer o Checkout falhar. Com **Bulkhead**, **Partição 1** (ex.: 20 threads) é dedicada a Relatórios e **Partição 2** (80 threads) a Checkout. O diagrama mostra as partições como caixas separadas. Se Relatórios travar, no máximo 20 threads são afetadas; as 80 de Checkout seguem disponíveis.`,

  'health-checks': `**Caso de uso:** o **orquestrador** (Kubernetes) e o **load balancer** precisam saber se o processo está vivo e se pode receber tráfego. **Liveness**: se falhar, o container é reiniciado (processo travou). **Readiness**: se falhar, o pod é removido do Service (não recebe novas requests até estar pronto — ex.: banco conectado). O diagrama mostra Liveness → App → Readiness. Na prática, o app expõe \`/live\` (leve) e \`/ready\` (pode incluir checagem de DB). Graceful shutdown: falhar readiness, esperar requests em flight, depois sair.`,
}
